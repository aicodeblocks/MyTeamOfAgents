# OpenClaw setup notes, 2026-04-14

This captures the working setup we finished today.

## 1) Fix local CLI pairing

Use the local CLI to complete pairing and confirm the node is reachable.

```bash
openclaw gateway status
openclaw help
```

Mansoor then completed the missing successful repair step explicitly with:

```bash
openclaw devices approve --latest
```

That approval fixed the pending pairing/repair state and restored normal access.

## 2) Confirm the repair approval cleared the issue

We had a pending repair/reauthorization for the device. After `openclaw devices approve --latest`, the local pairing issue was cleared.

Practical checks:

```bash
openclaw gateway status
```

Confirm the device no longer shows as unauthorized or waiting for repair approval.

## 3) Verify cron access

After the pairing fix, Mansoor ran:

```bash
openclaw cron list
```

That returned the cron list successfully, showing there was currently no cron job configured. This confirmed cron access was working again after the approval step.

## 4) Create the daily briefing cron job

Install the daily briefing job in crontab.

```bash
crontab -e
```

Add the daily briefing entry using the agreed schedule/command for this workspace. After saving, verify it is present:

```bash
crontab -l
```

## 5) Enable subagent delegation allowlist

We updated delegation so Olivia can hand work to the allowed subagents:

- scout
- atlas
- forge
- sentinel
- ranger

The key outcome is that delegation is explicitly allowed for those agents instead of being blocked by default policy.

## 6) Restart the gateway

Apply the config change by restarting the gateway:

```bash
openclaw gateway restart
openclaw gateway status
```

Wait for the gateway to come back healthy before testing delegation.

## 7) Verify successful delegation

Run a real delegation test after restart. Success criteria:

- Olivia can delegate to the allowlisted agents
- the delegated task starts cleanly
- results return to the requester normally

## Olivia routing and delegation setup

For Olivia, keep routing/delegation configured so she can use the following workers:

- scout
- atlas
- forge
- sentinel
- ranger

Checklist:

1. Ensure Olivia's routing/delegation config includes those agent names in the allowlist.
2. Restart the gateway after changing the config.
3. Run one live delegation test.
4. Confirm the task executes and the result is returned end-to-end.

Useful verification commands:

```bash
openclaw gateway status
openclaw cron list
```

## Result

By the end of today’s work we had:

- local CLI pairing fixed
- pending device repair approved with `openclaw devices approve --latest`
- cron access verified with `openclaw cron list` (returned successfully, with no cron job yet)
- daily briefing cron job created
- Olivia delegation allowlist enabled for scout/atlas/forge/sentinel/ranger
- gateway restarted
- delegation working successfully
